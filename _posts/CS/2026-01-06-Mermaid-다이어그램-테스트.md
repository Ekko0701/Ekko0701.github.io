---
title: "Mermaid 다이어그램 렌더링 테스트"
categories:
  - CS

tags:
  - Mermaid
  - Test

---

안녕하세욥!

이 포스트는 mermaid 다이어그램이 제대로 렌더링되는지 테스트하기 위한 샘플 포스트임

여러 종류의 다이어그램을 넣어봤으니 잘 나오는지 확인해보자! 👀

 

# 플로우차트(Flowchart)

가장 기본적인 플로우차트부터!

```mermaid
graph TD
    A[시작] --> B{조건 확인}
    B -->|Yes| C[작업 실행]
    B -->|No| D[종료]
    C --> E[결과 저장]
    E --> D
    
    style A fill:#e1f5ff,stroke:#0066cc,stroke-width:2px,color:#000000
    style B fill:#fff3cd,stroke:#ffc107,stroke-width:2px,color:#000000
    style C fill:#d4edda,stroke:#28a745,stroke-width:2px,color:#000000
    style D fill:#f8d7da,stroke:#dc3545,stroke-width:2px,color:#000000
    style E fill:#d4edda,stroke:#28a745,stroke-width:2px,color:#000000
```

 

# 시퀀스 다이어그램(Sequence Diagram)

사용자와 시스템 간의 인터랙션을 표현해보자!

```mermaid
sequenceDiagram
    participant User as 사용자
    participant App as 앱
    participant API as API 서버
    participant DB as 데이터베이스
    
    User->>App: 로그인 요청
    App->>API: 인증 요청
    API->>DB: 사용자 정보 조회
    DB-->>API: 사용자 데이터 반환
    API-->>App: 토큰 발급
    App-->>User: 로그인 성공!
```

 

# 상태 다이어그램(State Diagram)

앱의 상태 전환 과정임

```mermaid
stateDiagram-v2
    [*] --> Idle
    Idle --> Loading: 데이터 요청
    Loading --> Success: 성공
    Loading --> Error: 실패
    Success --> Idle: 완료
    Error --> Idle: 재시도
    Success --> [*]
    Error --> [*]
```

 

# 클래스 다이어그램(Class Diagram)

객체 지향 설계를 표현해보자!

```mermaid
classDiagram
    class Animal {
        +String name
        +int age
        +makeSound()
        +eat()
    }
    class Dog {
        +String breed
        +bark()
        +fetch()
    }
    class Cat {
        +String color
        +meow()
        +scratch()
    }
    Animal <|-- Dog
    Animal <|-- Cat
```

 

# 간트 차트(Gantt Chart)

프로젝트 일정 관리용!

```mermaid
gantt
    title 프로젝트 개발 일정
    dateFormat YYYY-MM-DD
    section 기획
    요구사항 분석    :done, 2026-01-01, 3d
    설계             :done, 2026-01-04, 4d
    section 개발
    프론트엔드 개발   :active, 2026-01-08, 10d
    백엔드 개발       :active, 2026-01-08, 12d
    section 테스트
    통합 테스트       :2026-01-20, 5d
    배포             :2026-01-25, 2d
```

 

# 파이 차트(Pie Chart)

비율 표현!

```mermaid
pie title 하루 24시간 사용 현황
    "공부" : 6
    "개발" : 8
    "운동" : 2
    "식사" : 2
    "수면" : 6
```

 

# ER 다이어그램(Entity Relationship)

데이터베이스 설계!

```mermaid
erDiagram
    USER ||--o{ POST : writes
    USER ||--o{ COMMENT : writes
    POST ||--o{ COMMENT : has
    
    USER {
        int id PK
        string name
        string email
        datetime created_at
    }
    POST {
        int id PK
        int user_id FK
        string title
        string content
        datetime created_at
    }
    COMMENT {
        int id PK
        int user_id FK
        int post_id FK
        string content
        datetime created_at
    }
```

 

# 마인드맵(Mind Map)

아이디어 정리용!

```mermaid
mindmap
  root((블로그 운영))
    콘텐츠
      CS 공부
      iOS 개발
      Spring 공부
      알고리즘
    디자인
      UI/UX 개선
      다크모드
      반응형
    기능
      검색
      댓글
      태그
    홍보
      SEO 최적화
      소셜미디어
      커뮤니티
```

 

# 깃 그래프(Git Graph)

Git 브랜치 전략 시각화!

```mermaid
gitGraph
    commit
    commit
    branch develop
    checkout develop
    commit
    commit
    branch feature
    checkout feature
    commit
    commit
    checkout develop
    merge feature
    checkout main
    merge develop
    commit
```

 

.

.

.

오늘은 mermaid 다이어그램 렌더링 테스트를 해봤음

여러 종류의 다이어그램들이 제대로 나온다면 성공! 🎉

포스트 작성할 때 이런 다이어그램들을 활용하면 훨씬 이해하기 쉬울 듯 ㅎㅎ

 

다이어그램이 제대로 안 나온다면... 설정을 다시 봐야겠지? 👀

피드백 대환영!

